apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cinema-gateway
  namespace: cinema
spec:
  gatewayClassName: nginx
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    hostname: "cinema.local"
    allowedRoutes:
      namespaces:
        from: Same