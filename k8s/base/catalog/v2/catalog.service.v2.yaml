apiVersion: v1
kind: Service
metadata:
  name: catalog-service-v2
  namespace: cinema
spec:
  selector:
    app: catalog-service
    version: v2
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP